const nome = 'Lucas'; //String
const nome1 = "Lucas"; //String
const nome2 = `Lucas`; //string
const num2 = 10; // number
const num1 = 10.52; // number
let nomeAluno; // declarou a variável, mas, não inicializou ela. undefined= não aponta pra local nenhum na memória
let sobreNomeAluno = null; // nulo -> não aponta pra local nenhum na memória
const aprovado = false; // boolean = true, false (lógico). verdadeiro ou falso

console.log(typeof num1)