 var nome = 'Lucas';
 var nome = 'freitas';

 console.log(nome);
 // a grande diferencia de var para let, é que a variável var pode ser subistituidade da forma que foi feita que nao ocorrerá erro.

 //let pode ser alterado porém nao pode usar a tag let, apenas "nome" ou a palavra que vc usar para indentificar a variável.
 
 nome = 'Alves';
 console.log(nome); // pode ser feita assim tambem , porém, NÃO é recomendado, pois vai criar uma variável mundial e pode afetar outras variáveis do seu sistema.